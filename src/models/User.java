package models;

/**
 * Base model representing a generic user in the system.
 * Responsibility: Holds common attributes and behaviors shared across all user types (e.g., ID, name, contact info).
 */
public abstract class User {
    // Shared user attributes
}
