package strategies;

/**
 * Strategy interface for matching riders with drivers.
 * Responsibility: Defines a contract for driver matching algorithms.
 * Implementations might match based on closest distance, highest driver rating, etc.
 */
public interface MatchingStrategy {
    // Driver findMatch(Rider rider, Location pickupLocation);
}
