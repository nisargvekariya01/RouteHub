package strategies;

/**
 * Strategy interface for calculating ride fares.
 * Responsibility: Defines a contract for different pricing models.
 * Implementations might include standard pricing, surge pricing, or distance-based pricing.
 */
public interface PricingStrategy {
    // double calculateFare(Ride ride);
}
