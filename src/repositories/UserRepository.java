package repositories;

/**
 * Repository interface/class for User data.
 * Responsibility: Abstracts the storage mechanism for User objects (Riders and Drivers).
 * Provides methods for saving, retrieving, and updating user records (e.g., in-memory collection).
 */
public class UserRepository {
    // Storage and retrieval methods for Users
}
